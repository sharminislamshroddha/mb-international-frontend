# Frontend Conventions

## Layout

- Always wrap page content inside `Container`.
- Never use `max-w-*` directly in pages.
- Use `Container` for consistent spacing.

## Imports

Always use aliases:

```ts
import Container from "@/components/layout/Container";