NEXT_PUBLIC_API_URL="http://localhost:5001/api/v1"

# Google OAuth client ID (same project as the backend's GOOGLE_CLIENT_ID) — leave unset to disable
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""

# Facebook App ID (same app as the backend's FACEBOOK_APP_ID) — leave unset to disable
NEXT_PUBLIC_FACEBOOK_APP_ID=""
